export default class Process {

    halfproductsInputCount() {
        for(this.halfproducts of halfproduct) {
            halfproduct
        }
    }

    constructor(outputCount, halfproducts, materials, wastePersent, wasteCount) {
        this.halfproducts = halfproducts;   // Полуфабрикаты
        this.materials = materials;         // Материалы
        this.wastePersent = wastePersent;   // Процент техотходов
        this.wasteCount = wasteCount;       // Количество техотходов
        this.outputCount = outputCount;     // Выходной тираж
    }
}